#include "building.h"

/**
 * @brief Building::Building constructor for parent class Building
 * @param type is the enum class type of the building
 */
Building::Building(BuildingType type)
{
    type_ = type;

}
